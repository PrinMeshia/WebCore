component CounterPage {
  state {
    count: Int = 0
  }
  
  view {
    h1 "🔢 Mon Compteur WebCore"
    p "Nombre de clics: {count}"
    
    button on:click={ count += 1 } "Increment"
    button on:click={ count = max(0, count - 1) } "Decrement"
  }
}
